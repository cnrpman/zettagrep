# 2026-04-13 TODOs

## Highest Priority

- 建一套 search quality fixtures / goldens，用固定 query 集和期望结果评估每次搜索改动是否真的变好。
- 做 ranking / routing 调优，明确 docs chunk、code symbol chunk、name hit、意群 hit 的排序权重。
- 把 `zg index status` 做成真正可诊断的面板：区分 text chunk / symbol chunk、语言分布、跳过原因、shared embedding 复用情况。

## Next

- 继续补 code symbol 覆盖：在保持低复杂度前提下补更多常见 symbol 形态。
- 后续补 C / C++ / Java 的 code symbol extraction。
- 提升 doc 提取质量，尤其是 Python docstring、JS/TS 注释块、Rust doc comment 的覆盖稳定性。

## Release

- 把 Homebrew 从 `head` formula 推进到 stable formula。
- 准备 release tarball 与 `sha256`。
- 把 `ripgrep` runtime dependency 和安装路径写清楚。

## UX

- 搜索结果展示区分 doc hit / symbol hit。
- 给 symbol 结果展示 `language` / `kind` / `container`。
- 改善 snippet 裁剪和高亮。
