# MEMORY

Purpose: capture user displeasure moments and stable taste signals so future iterations match the user's expectations faster.

- High-level tests, e.g. overall behavior, concurrency tests should live under `tests/`, not be buried inside implementation files as if they were only unit tests.
